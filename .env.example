# JARVIS AI Assistant Configuration

# AI Provider: "ollama" (local, free) or "gemini" (cloud, free but needs API key)
AI_PROVIDER=ollama

# Ollama Model (if using ollama) - llama3.2, mistral, phi, etc.
OLLAMA_MODEL=llama3.2

# Google Gemini API Key (only needed if AI_PROVIDER=gemini)
GEMINI_API_KEY=your_api_key_here

# Get Gemini API key from: https://aistudio.google.com/app/apikey
